// wildcard
* {
  // new box border style.  resets dimensions in browser. 
  box-sizing: border-box;
  border: 1px solid red;
  // This is called resetting the style sheet in the browser's default settings for user-agent. 
  margin: 0;
  padding: 0;
}

display: inline-block, block, float

//Visibility takes hidden, visible, collapse
.future {
  visibility: hidden;
}

// Note: What’s the difference between display: none and visibility: hidden? 
// An element with display: none will be completely removed from the web page. 
// An element with visibility: hidden, however, will not be visible on the web page, but the space reserved for it will.

p {
  color: green;
}

h1 {
  color: maroon;
}

.title {
  color: teal;
}

.uppercase {
  text-transform: uppercase;
}

#article-title {
  font-family: cursive;
}
// find any link with a image containing the word florence.  
a[href*='florence'] {
  color: lightgreen;
}

a[href*='beijing'] {
  color: lightblue;
}

a[href*='seoul'] {
  color: lightpink;
}

a:hover {
  color:darkorange;
}
.heading-background {
  background-color: aqua;
}

#publish-time {
  color: gray;
}
